# Hello World Page

This static page is generated by the local Agent Team runtime.

- Research Agent creates a learning report.
- Web Agent turns the report into HTML/CSS/JS.
- The deploy flow pushes the static site to the GitHub `gh-pages` branch.
